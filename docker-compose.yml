services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data/uploads:/app/data/uploads
      - ./data/chroma:/app/data/chroma
